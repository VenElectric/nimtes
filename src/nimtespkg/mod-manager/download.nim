# intercept nxm downloads
# and then download them to quarantine
# once in quarantine, run conflict detection on existing files

# hard to find information on how to handle nxm links
# will need to do more research
# to unstick from this...will manually download for now. 

# create snapshots when user wants
# always create a snapshot of the OG morrowind files (if able)
# allow different configs of files. i.e. PluginSet1 PluginSet2 (save these as snapshots)
# keep track of where each of the plugin files are (and if they update an existing vanilla file)
# vanilla meaning morrowind GOTY + officially released plugins
# download plugin to "quarantine" temporary folder and compare against existing file set. 
# ensure that files referenced in plugin are available either in vanilla Morrowind or the plugin itself
# some plugins may have different settings, ensure to search through those folders as well.
